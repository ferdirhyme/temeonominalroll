import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ferdIT.tmeonominal',
  appName: 'Tmeo Nominal Roll',
  webDir: 'dist'
};

export default config;
